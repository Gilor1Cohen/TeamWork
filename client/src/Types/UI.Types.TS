interface BtnOneProps {
  text: string;
  isDisabled: boolean;
  link: string;
}

interface BoxDataProps {
  title: string;
  imageName: string;
  description: string;
}

export type { BtnOneProps, BoxDataProps };
